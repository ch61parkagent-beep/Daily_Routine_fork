# 출근버스 30분 — 데일리 루틴 페이지

출근 버스에서 보내는 30분을 위한 단일 HTML 페이지. 매일 쓰는 개인 루틴 도구다.

**현재 상태: 제작 완료, 매일 사용 중.**

만들던 중이 아니라 **완성돼 돌아가는 도구다.** 그래서 변경은 보수적으로 한다 —
개선 제안을 하기 전에 "지금 불편한 점이 실제로 있는가"를 먼저 확인한다. 요청받지
않은 리팩터링이나 기능 추가는 하지 않는다. 매일 쓰는 물건이라 **깨지면 바로 티가
나고**, 변경 후에는 반드시 브라우저에서 실제로 열어 확인한다.

## 구조

- `daily-routine.html` — 전부. HTML·CSS·JS가 한 파일에 있다(375줄).
- 빌드 없음, 의존성 없음, 서버 없음. 파일을 브라우저로 열면 그게 실행이다.

**이 단순함이 이 프로젝트의 기능이다.** 버스에서 바로 열려야 하므로 빌드
단계나 네트워크 의존성을 추가하지 않는다. 프레임워크를 넣자는 제안은 하지 않는다.

## 상태 저장

- 진행 상태는 `localStorage`에 남는다. 서버가 없으므로 이게 유일한 저장소다.
- **키 이름을 바꾸면 사용자의 기존 기록이 사라진다.** 이름을 바꿔야 하면
  이전 키를 읽어 옮기는 코드를 함께 넣는다.
- 시크릿 모드·다른 브라우저·기기 변경 시 기록이 없는 것은 정상이다. 그 상태에서도
  페이지가 정상 렌더돼야 한다.

## 스타일

- 클래스 이름이 의미 단위로 붙어 있다(`masthead`, `board`, `flow`, `chain`,
  `formula`, `oneliner`, `closing`, `datepill`). 새 요소를 넣을 때 이 어휘를 따른다.
- 버스에서 한 손으로 보는 화면이다. **모바일 세로 화면이 기준**이고 글자 크기와
  터치 영역을 그에 맞춘다.

## 아직 없는 것

- 여러 기기 간 동기화 (localStorage 특성상 불가 — 필요해지면 설계가 달라진다)
- 테스트 (단일 파일이라 눈으로 확인하는 것이 현재 방식)
